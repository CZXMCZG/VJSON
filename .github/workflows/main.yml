name: Elite Node Hunter

on:
  schedule:
    - cron: '0 */4 * * *' 
  workflow_dispatch:

jobs:
  hunt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Tools
        run: |
          pip install aiohttp PyYAML

      - name: Run Hunter
        run: python elite_hunter.py > /dev/null 2>&1

      - name: Deliver
        run: |
          git config --local user.email "hunter@github.com"
          git config --local user.name "EliteHunter"
          git add assets_manifest.bin
          git commit -m "update elite assets [skip ci]" || exit 0
          git push > /dev/null 2>&1